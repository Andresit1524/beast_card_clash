{
    // Exclusiones de archivo
    "files.exclude": {
        "**/.godot/": true,
        "**/.builds/": true,
        "**/.doc_maker/": true,
    },

    // Regla para no pasar de los 100 caracteres en el código
    "editor.rulers": [100]
}